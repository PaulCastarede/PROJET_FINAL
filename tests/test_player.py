# Tests pour les mouvements du joueur
#test_player_movement_right() : #Vérifier que le joueur se déplace correctement vers la droite
#test_player_movement_left() : #Vérifier que le joueur se déplace correctement vers la gauche
#test_player_jump() : #Vérifier que le joueur saute avec la bonne vitesse
#test_player_gravity() : #Vérifier que la gravité s'applique correctement

# Tests pour la gestion des vies et du score
#test_player_initial_lives() : #Vérifier que le joueur commence avec INITIAL_PLAYER_LIVES vies
#test_player_death() : #Vérifier que le joueur perd une vie quand il touche un monstre ou tombe
#test_player_respawn() : #Vérifier que le joueur réapparaît au bon endroit après une mort
#test_player_game_over() : #Vérifier que le jeu se termine quand les vies atteignent 0
#test_player_collect_coins() : #Vérifier que le score augmente correctement avec les pièces
#test_player_extra_life() : #Vérifier qu'une vie supplémentaire est donnée tous les 10 points